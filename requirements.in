# Approved direct production dependencies. Changes here are deliberate version
# upgrades; regenerate requirements.txt with the exact uv command in its header.
# The generated lock is the only file installed into production images.
fastapi==0.139.0
uvicorn[standard]==0.51.0
python-multipart==0.0.32
pydantic-settings==2.14.2
numpy==2.5.1
litellm==1.84.0
psycopg[binary]==3.3.4
pgvector==0.5.0
alembic==1.18.5
cryptography==49.0.0

# Document extraction
pypdf==6.14.2
python-docx==1.2.0
openpyxl==3.1.5
python-pptx==1.0.2
striprtf==0.0.32
pymupdf==1.28.0
pytesseract==0.3.13
pillow==12.3.0

# Transitive security floors. Keep these explicit until the direct dependency
# constraints make the audited fixed versions unavoidable.
aiohttp==3.14.1
click==8.3.3
python-dotenv==1.2.2
