"""Accounting (Buchhaltung) module: German-first invoice bookkeeping.

An optional, per-workspace product module built on the same catalogue template as
KPI Dashboard and AI Employees (no separate container; gated by an
``AppInstallation``). Phase 0 ships only the skeleton — see ``app/accounting/base``.
"""
